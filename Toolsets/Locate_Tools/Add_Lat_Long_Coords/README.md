This tool takes an input point feature layer and simply adds XY coordinates to each point.
It will add a "Longitude" & "Latitude" field if they do not exist already, then it will populate those fields with the appropriate coordinate values.
