Creates a feature layer (not feature class) for each unique value. Useful to separate values into different layers before publishing to AGOL. 
The output goes to the first map frame in the index.. if the output doesnt show up in your current map view, read the output messages to see which map the layer is in. 
This is set up this way due to a bug. 
