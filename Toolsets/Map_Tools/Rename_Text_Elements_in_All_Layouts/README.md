Renames all text elements in all layouts to what the text is. 
For example, if a text elem in a layout has the words "Light St", but in the contents it is called "Text 34", then that element's title will be changed to "Light St"
